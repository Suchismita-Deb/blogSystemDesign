CamelCamelCamel an application that monitor amazon price and alert the user when the price drop below the threshold.

### Solve By Myself.

Non-Functional Requirements - The system should be available meaning user should see the price a latency of 500 ms is fine atleast the user will be able to see the data.
Latency 500 ms .
availability more than consistency.
System should support 1m users.                 

Issue - I did not mention the alert when the price drops. 
System scale to 1 M is correct but point it to the read and write of the application specific.